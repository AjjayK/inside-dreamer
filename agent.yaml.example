# Agent Configuration Template
# This file shows the structure of agent.yaml with its configuration options

agentId: your-agent-id-here
name: Your Agent Name
hasUi: true

# Default view provided by the agent (widget | app | feed) - indicates how it is shown in the gallery
defaultView: widget

# Optional tools configuration
# Tools used in code are automatically detected as "required" during push.
# List server names here to mark them as "optional" instead - users can enable/disable these.
# Example: optionalTools: ["slack", "linear"]
optionalTools: []

# UI Views configuration options
views:
  - type: app
  - type: widget
    name: Single Note
    identifier: note-widget
    constraints:
      minWidth: 72
      minHeight: 168
      maxWidth: 360
      maxHeight: 360
      defaultWidth: 168
      defaultHeight: 168
  - type: feed

# Optional triggers configuration
triggers:
  - type: cron
    defaultSchedule: "0 */2 * * *"  # Every 2 hours
    entrypoint: "main"  # Optional - defaults to 'main'
    name: "Check for Updates"  # Optional - custom name for the trigger

  - type: input
    contentTypes: ["text/uri-list", "text/markdown"]
    entrypoint: "processText"
    name: "Process URLs and Markdown"  # Optional - custom name for the trigger
